import { useState } from 'react';
import { AlertCircle, CheckCircle2, UserPlus } from 'lucide-react';
import api from '../../services/api';

const initialForm = { name: '', email: '', password: '', role: 'student', rollNumber: '', department: 'Computer Science', designation: 'Assistant Professor', semester: '1' };
const inputClass = 'w-full rounded-xl border border-slate-700 bg-slate-950 px-3 py-2.5 text-sm text-white placeholder:text-slate-600 focus:border-sky-500 focus:outline-none focus:ring-1 focus:ring-sky-500';

export default function UserRegistrationForm({ onUserCreated }) {
  const [formData, setFormData] = useState(initialForm); const [loading, setLoading] = useState(false); const [message, setMessage] = useState(null); const isStudent = formData.role === 'student';
  const handleChange = ({ target: { name, value } }) => setFormData((current) => ({ ...current, [name]: value }));
  const handleSubmit = async (event) => { event.preventDefault(); setLoading(true); setMessage(null); try { const payload = { ...formData, semester: Number(formData.semester) }; if (!isStudent) delete payload.rollNumber; const { data } = await api.post('/admin/users', payload); setMessage({ type: 'success', text: data.message || 'Account created successfully.' }); setFormData(initialForm); onUserCreated?.(data.data); } catch (error) { setMessage({ type: 'error', text: error.response?.data?.message || 'Unable to create the account. Please try again.' }); } finally { setLoading(false); } };
  return <div className="overflow-hidden rounded-2xl border border-slate-800 bg-slate-900 shadow-xl shadow-slate-950/20"><div className="border-b border-slate-800 bg-slate-900/70 px-6 py-5"><div className="flex items-center gap-3"><div className="rounded-xl bg-sky-500/10 p-2.5 text-sky-400"><UserPlus className="h-5 w-5" /></div><div><h2 className="font-bold text-white">Provision a campus account</h2><p className="mt-0.5 text-xs text-slate-400">Student and faculty accounts are created with secure login credentials.</p></div></div></div><form onSubmit={handleSubmit} className="space-y-5 p-6">{message && <div role="alert" className={`flex items-start gap-2 rounded-xl border p-3 text-sm ${message.type === 'success' ? 'border-emerald-500/30 bg-emerald-500/10 text-emerald-300' : 'border-rose-500/30 bg-rose-500/10 text-rose-300'}`}>{message.type === 'success' ? <CheckCircle2 className="mt-0.5 h-4 w-4 shrink-0" /> : <AlertCircle className="mt-0.5 h-4 w-4 shrink-0" />}<span>{message.text}</span></div>}<div><label className="mb-2 block text-xs font-semibold uppercase tracking-wider text-slate-400">Account role</label><div className="grid grid-cols-2 gap-3">{['student', 'faculty'].map((role) => <button key={role} type="button" onClick={() => setFormData((current) => ({ ...current, role }))} className={`rounded-xl border px-4 py-3 text-sm font-semibold capitalize transition ${formData.role === role ? 'border-sky-500 bg-sky-500/10 text-sky-300' : 'border-slate-700 bg-slate-950 text-slate-400 hover:border-slate-600'}`}>{role}</button>)}</div></div><div className="grid gap-4 md:grid-cols-2"><Field label="Full name"><input className={inputClass} name="name" required value={formData.name} onChange={handleChange} placeholder="e.g. Priya Sharma" /></Field><Field label="Campus email"><input className={inputClass} type="email" name="email" required value={formData.email} onChange={handleChange} placeholder="priya@campus.edu" /></Field></div><Field label="Temporary password" hint="At least 6 characters"><input className={inputClass} type="password" name="password" required minLength="6" value={formData.password} onChange={handleChange} placeholder="Set an initial password" /></Field><div className="border-t border-slate-800 pt-5"><p className="mb-4 text-xs font-semibold uppercase tracking-wider text-slate-400">{isStudent ? 'Student details' : 'Faculty details'}</p><div className={`grid gap-4 ${isStudent ? 'md:grid-cols-3' : 'md:grid-cols-2'}`}>{isStudent && <Field label="Roll number"><input className={inputClass} name="rollNumber" required value={formData.rollNumber} onChange={handleChange} placeholder="CS2026-001" /></Field>}<Field label="Department"><input className={inputClass} name="department" required value={formData.department} onChange={handleChange} /></Field>{isStudent ? <Field label="Semester"><input className={inputClass} type="number" name="semester" min="1" max="8" required value={formData.semester} onChange={handleChange} /></Field> : <Field label="Designation"><select className={inputClass} name="designation" value={formData.designation} onChange={handleChange}><option>Assistant Professor</option><option>Associate Professor</option><option>Professor</option><option>Head of Department</option></select></Field>}</div></div><button disabled={loading} className="flex w-full items-center justify-center gap-2 rounded-xl bg-sky-600 px-4 py-3 text-sm font-semibold text-white shadow-lg shadow-sky-950/30 transition hover:bg-sky-500 disabled:cursor-not-allowed disabled:opacity-60"><UserPlus className="h-4 w-4" />{loading ? 'Creating account…' : `Register ${isStudent ? 'student' : 'faculty member'}`}</button></form></div>;
}
function Field({ label, hint, children }) { return <label className="block text-sm font-medium text-slate-300"><span className="mb-2 flex justify-between gap-2"><span>{label}</span>{hint && <span className="text-xs font-normal text-slate-500">{hint}</span>}</span>{children}</label>; }
